  int len = str.length();
        String result = "";
        if (len > 3) {
            result = str.substring(0, len - 3) + str.substring(len - 3, len).toUpperCase();

        } else {
            result = str.toUpperCase();
        }return result;
